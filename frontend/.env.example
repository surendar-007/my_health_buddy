# Base URL of the Flask backend. Use "/api" to route through the Vite dev proxy
# (configured in vite.config.ts), or a full URL like http://localhost:5000.
VITE_API_BASE_URL=/api

# Set to "false" to call the real Flask backend instead of the built-in mock data.
VITE_USE_MOCK=true
